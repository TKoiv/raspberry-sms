import random
import sqlite_user

class PinCode:


    #Call this function read your PIN-code and compare it whit password. 
    def pin():
        lockCounter = 0
        while True:
            pinCode = []
            pin = 0
            #print("Give your pin-code now")
            try:
                #print(password)
                i = int(input("Give 4 numbers PIN-CODE:"))
                pinCode.append(i)

                for pin in pinCode:
                    pin = pin
                #print(pin)
                password = sqlite_user.getUser(pin)
                #print(password)
                if password == 0:
                    print("Wrong pin-code try again!")
                    lockCounter += 1
                    print(lockCounter)
                elif pin == password:
                    print("Open lock!")
                    lockCounter = 0
                    break
                if lockCounter == 3:
                    print("Kirjautuminen estetty")
                    return lockCounter
                    break 
            except ValueError:
                continue  
    #pin()

